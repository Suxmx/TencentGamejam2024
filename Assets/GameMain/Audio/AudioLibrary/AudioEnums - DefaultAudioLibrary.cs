namespace GameMain {
    public enum GameplayAudio {
        BulletHit,
        GetKey,
        JumpStart,
        Land,
        NoBullet
    }
}
    public enum DefaultAudioLibraryMusic {
        Bgm
    }
